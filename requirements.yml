name: limmat-env
channels:
  - defaults
  - conda-forge
  - gurobi
dependencies:
  - pip
  - ipython
  - requests
  - beautifulsoup4
  - pandas
  - numpy
  - matplotlib
  - pip:
      - openmeteo_requests
      - requests_cache
      - retry_requests
     